file(GLOB MPS_HEADER_FILES
  RELATIVE ${PROJECT_SOURCE_DIR}/include
  CONFIGURE_DEPENDS
  LIST_DIRECTORIES false
  mps/*.h
  mps/*.hpp
  )

install(
  DIRECTORY ${PROJECT_SOURCE_DIR}/include/mps
  DESTINATION ${CMAKE_INSTALL_INCLUDEDIR}
  )
