include(CMakeFindDependencyMacro)

# This is a list of find_dependency() search statements created
# during configuration
if(NOT tensor_FOUND)
  find_dependency(tensor)
endif()

# Add the targets file
include("${CMAKE_CURRENT_LIST_DIR}/mpsTargets.cmake")
